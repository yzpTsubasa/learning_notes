@Library("jenkins_library") _

def build = new org.devops.build()

pipeline {
    agent any
    triggers {
        pollSCM 'H/2 * * * *'
    }
    options {
        disableConcurrentBuilds()
        ansiColor('xterm')
    }

    stages {
        stage('generate_translation') {
            steps {
                script {
                    build.generateTranslationKV_API()
                }
            }
        }
    }
    
    post { 
        failure { 
            script {
                build.sendCommonResult2DingTalk()
            }
        }
    }
}
